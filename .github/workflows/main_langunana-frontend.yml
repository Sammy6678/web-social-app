name: Build and Deploy React App to Azure WebApp - langunana-frontend

on:
  push:
    branches:
      - main   # 👈 apni branch ka naam daalna (agar alag hai to)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'   # Azure App Service supported version

      - name: Install dependencies & Build React
        working-directory: frontend   # 👈 frontend folder ke andar
        run: |
          npm ci
          npm run build

      - name: Upload production-ready dist
        uses: actions/upload-artifact@v4
        with:
          name: react-dist
          path: frontend/dist/*   # 👈 sirf dist ka content upload hoga

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download dist artifact
        uses: actions/download-artifact@v4
        with:
          name: react-dist
          path: ./dist

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_FB87C442A6AF4CAAB85753530DB5E1A7 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_15F438C3ABE14310819B90429B7FA31C }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_745609D10A8B45969AC1E92CF58B8908 }}

      - name: Deploy to Azure WebApp
        uses: azure/webapps-deploy@v2
        with:
          app-name: "YOUR_AZURE_APP_NAME"   # 👈 apna Azure Web App ka naam likhna
          slot-name: "production"
          package: ./dist
